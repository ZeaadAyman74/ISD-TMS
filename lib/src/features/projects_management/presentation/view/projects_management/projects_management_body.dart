import 'package:flutter/material.dart';

class ProjectsManagementBody extends StatelessWidget {
  const ProjectsManagementBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
