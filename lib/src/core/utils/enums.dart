enum ToastStates {success,error,warning}

enum TimeSlotStatus {past,available,duration,booked,staffHoliday}

enum BookingStatus {
  pending, // orange
  scheduled, //blue
  completed, // green
  declined //red
}

enum NotificationType { general , reminder, bookingDeclined, bookingApproved, bookingRescheduled}

